# 次回対応予定

- 最終更新: 2026-08-20
- 基準コミット: `c0f58a6`

この文書は、次回の開発作業へ引き継ぐための優先順位付きロードマップです。
本ソフトは引き続き、ディスクイメージと内部ファイルシステムを変更しない読み取り専用ツールとして実装します。

## 次回の推奨作業

### 1. イメージ読み込み処理のキャンセル

大容量イメージを開いたとき、次の処理を途中で安全にキャンセルできるようにします。

- LZO高速モードの一時RAW展開
- LZO省容量モードの索引作成
- OVAの一時展開
- VMAの解析
- パーティションおよびファイルシステム解析

実装方針:

- 検索・コピーとは別の読み込み専用`CancellationTokenSource`を使用する
- 画面上部に「読み込みキャンセル」を表示する
- キャンセル確認を各長時間ループへ追加する
- キャンセルまたは失敗時は、作成途中の一時ファイルと専用一時フォルダーを削除する
- 新しいイメージの読み込みに失敗しても、可能な限り現在表示中のイメージを維持する
- アプリ終了時も同じキャンセル・後処理経路を利用する

完了条件:

- LZO展開中、OVA展開中、解析中の各段階でキャンセルできる
- キャンセル後に不完全な一時ファイルが残らない
- 検索キャンセル、コピーキャンセル、読み込みキャンセルが互いに干渉しない
- 自動テストでキャンセルと一時ファイル削除を確認できる

### 2. BitLocker 48桁回復パスワード対応

既存のBitLockerメタデータ解析、クリアキー解除、生FVEK、AES-XTS復号処理を利用し、一般的な48桁回復パスワードから読み取り専用でボリュームを開けるようにします。

実装方針:

- 回復パスワード保護子を検出した場合に入力ダイアログを表示する
- 8組の6桁ブロックとして正規化・検証する
- 各ブロックのBitLockerチェック条件を検証し、入力ミスを具体的に表示する
- 回復キーを導出し、保護されたVMKとFVEKをAES-CCMで解除する
- 復号後は既存の`BitLockerDecryptingReader`へ接続する
- 回復パスワード、VMK、FVEKを設定・ログ・解析レポートへ保存しない
- 不要になったキーのバイト配列は可能な範囲で消去する
- 上級者向けに生FVEK入力UIも追加候補とする

対象外:

- TPM秘密情報を持たないTPM単独保護のオフライン解除
- BitLockerボリュームの書き換え、保護子の追加・削除
- 回復パスワードのクラウドやActive Directoryからの自動取得

完了条件:

- 正しい48桁回復パスワードでNTFS/exFATの一覧・プレビュー・コピーができる
- 誤った回復パスワードを安全に拒否できる
- 回復パスワードや復号キーがログとレポートに含まれない
- クリアキーによる既存の自動解除を壊さない

### 3. LZO高速モードのキャッシュ再利用

同じ大容量`.dd.lzo`を開くたびに全体展開する時間を減らします。

追加する選択肢:

- 終了時に一時RAWを削除（現在の動作）
- 次回利用のためキャッシュとして保持
- ユーザー指定場所へ通常のRAWとして保存

実装方針:

- 元LZOのフルパス、ファイルサイズ、更新日時、識別情報をキャッシュメタデータへ保存する
- 展開完了前は「未完成」と記録し、未完成キャッシュを再利用しない
- 元LZOが変更された場合はキャッシュを無効化する
- 再利用前にRAWサイズとメタデータを検証する
- UIからキャッシュ一覧、使用容量、削除を管理できるようにする
- 保存先の空き容量を展開前に確認・確保する

完了条件:

- 変更されていないLZOは再展開せず開ける
- 変更されたLZOや不完全なキャッシュを誤って再利用しない
- ユーザーがキャッシュ保存先と削除方針を制御できる

## 保守・品質改善

機能追加と並行して、次を独立したコミットで行います。

- DiscUtils系パッケージを`1.0.84`から更新候補`1.0.88`へ上げ、全テストを実行する
- GitHub ActionsでWindows上のDebugビルドとテストを自動実行する
- 破損・途中切れ・異常サイズの入力テストを増やす
- XFS bigtime、BitLocker、LZOキャンセルなどの回帰テストを追加する
- 外部由来の実イメージは、再配布条件と機密情報を確認してからテスト資産へ追加する

## 将来の対応形式候補

利用目的に応じ、次の順で検討します。

1. LUKS1/LUKS2
   - Linux暗号化パーティションとLUKS暗号化qcow2を対象とする
   - パスフレーズを保存・ログ出力しない
2. E01/EWF
   - フォレンジック用途向け
   - 分割セグメント、圧縮、整合性情報、ライセンスと配布方法を事前調査する
3. Btrfs
   - サブボリューム、圧縮、チェックサム、複数デバイスを段階的に扱う
4. Linux md RAIDの実読み取り
   - まずRAID1から開始し、その後RAID0/5/6を検討する
5. LVM2の拡張
   - 複数PV、thin、snapshot、cache、mirror、RAID segmentを段階的に対応する

## 維持する既存仕様

- ディスクイメージと内部ファイルシステムは読み取り専用
- 選択コピーと表示フォルダーコピーではハッシュ計算を行わない
- コピーはバックグラウンドキューで処理し、追加コピーで既存コピーをキャンセルしない
- 検索、コピー、読み込みには独立したキャンセルトークンを使用する
- コピー中はプログレスバー、転送速度、推定残り時間を表示する
- 動作中の処理を、新しいビルドやテストのために強制終了しない

## 次回開始時の確認手順

1. `git fetch origin`を実行し、`main`と`origin/main`の差分を確認する
2. 作業ツリーにユーザーの未コミット変更がないか確認する
3. `dotnet build Qcow2Explorer.sln --configuration Debug`を実行する
4. `dotnet run --project tests\Qcow2Explorer.Tests\Qcow2Explorer.Tests.csproj --configuration Debug --no-build`を実行する
5. 上記の「1. イメージ読み込み処理のキャンセル」から着手する
